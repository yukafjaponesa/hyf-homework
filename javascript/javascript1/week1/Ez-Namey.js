const firstWords = ["Easy", "Awesome", "Corporate", "Bright", "Advanced"];
const secondWords = ["Life", "Action", "Together", "Way", "Future"];

const startupName = ;

function startupName () {
  Math.floor(Math.random())
  console.log(startupName(firstWords, secondWords))
};
